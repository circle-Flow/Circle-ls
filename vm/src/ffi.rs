// FFI stub - intended to provide plugin / native bindings between VM and Rust/C# modules.
// For now it's empty and reserved for future expansion.

pub fn register_native_fn(_name: &str) {
    // register host function
}
